
const playerOperator = "";
const playerNumA = 0;
const playerNumB =0;

function userInteraction(operator, numA, numB) {

 while(playerOperator !== "/" || playerOperator !== "+" || playerOperator !== "-" || playerOperator === ! "*") {
  prompt("entre com o valor A");
  }

}
